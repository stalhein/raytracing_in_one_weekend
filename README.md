# Raytracing in One Weekend implementation in Rust
Learning the Rust programming language by following the Raytracing in One Weekend tutorial/book but converting from C++ to Rust.

## Compilation steps
Cargo and Rust must be installed on the system before running ```cargo run```.
